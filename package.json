{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm --prefix server start",
    "build": "npm install --include=dev --prefix server && npm install --include=dev --prefix client && npm run build --prefix client",
    "lint": "eslint .",
    "test": "npm test --prefix client && npm test --prefix server"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^8.0.0",
    "globals": "^17.12.0"
  },
  "dependencies": {
    "eslint": "^10.10.0",
    "zustand": "^5.0.15"
  }
}
